# Example environment configuration for the Chatter BBS terminal bridge
# Copy this file to .env or export the variables before launching the server.

# Protocol options: ssh (default) or telnet
CHATTER_BBS_PROTOCOL=ssh

# Hostname or IP address of the upstream BBS/telnet server
CHATTER_BBS_HOST=bbs.example.com

# Port exposed by the upstream server (22 for SSH, 23 for telnet)
CHATTER_BBS_PORT=22

# Optional defaults that appear in the connection drawer when no server host is set
CHATTER_BBS_HOST_DEFAULT=bbs.example.com
CHATTER_BBS_PORT_DEFAULT=23

# Optional SSH presets: username prompt and command run after login
CHATTER_BBS_SSH_USER=bbsop
CHATTER_BBS_SSH_COMMAND="/usr/local/bin/chatter"

# Optional placeholder text shown in the host input when unset
CHATTER_BBS_HOST_PLACEHOLDER="dial telnet.example.com"
