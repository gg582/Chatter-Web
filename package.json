{
  "name": "chatter-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "node scripts/build.mjs",
    "start": "node dist/server.js",
    "test": "npm run build && node --test --experimental-specifier-resolution=node dist/tests/**/*.test.js"
  },
  "dependencies": {
    "@xterm/xterm": "^5.5.0",
    "@xterm/addon-fit": "^0.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  },
  "packageManager": "npm@10.5.0",
  "description": "A standalone TypeScript front-end for the Chatter BBS control deck. The project mirrors every `/help` command with a matching GUI workflow so the SSH client and the browser stay feature-aligned.",
  "main": "next.config.js",
  "directories": {
    "test": "tests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
